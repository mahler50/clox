# clox - A compiler of Lox in C

## How to run
Make sure you have `gcc` installed.

Run `make` in your terminal:
```sh
make
```

Or if you want to hack into this compiler, run `make debug` to generate a debuggable program.